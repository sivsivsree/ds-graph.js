# ds-graph.js

Graph Data Manipulation.

`
let g = new Graph();

g.addNode('a');
g.addNode('b');
g.addNode('c');
g.addNode('d');

g.addEdge('a', 'b');
g.addEdge('b', 'c');
g.addEdge('c', 'd');



g.relations();

`
